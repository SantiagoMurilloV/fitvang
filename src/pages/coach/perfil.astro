---
import RootLayout from '@/layouts/RootLayout.astro';
import Entry from '@/components/entry';
import { guardArea } from '@/lib/requireAuth';
const __guard = await guardArea(Astro.request, 'coach');
if (__guard.redirect) return Astro.redirect(__guard.redirect);
---
<RootLayout title="Fitvang — Coach · Perfil">
  <Entry client:load view="coach/perfil" user={__guard.user} />
</RootLayout>

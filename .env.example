# ---- API Backend (Railway) ----
# URL pública del backend en Railway
PUBLIC_API_URL=https://tu-api.railway.app

# ---- JWT (mismo secret que el backend para verificar tokens en SSR) ----
# Debe ser IDÉNTICO al JWT_SECRET del backend
JWT_SECRET=cambia-esto-en-prod
